# Modules is a way to reuse a code written by someone else

import math
print(math.sqrt(16))

# It gives the directory of the provided module
print(dir(math))

import numpy as np
pn = np.random
print(pn)
